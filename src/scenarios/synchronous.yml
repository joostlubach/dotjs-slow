name: Synchronous

code:
  etienne: |-
    etienne.onHungry = function () {
      const server = waitInLine()

      const hamburger = server.order([
        'cheese',
        'tomato',
        'bacon'
      ])
      this.eat(hamburger)  
    }

  server:
  chef: