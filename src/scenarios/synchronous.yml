name: Synchronous

code:
  etienne: |-
    etienne.onHungry = function () {
      const server = restaurant.waitInLine()

      const hamburger = marie.order([
        'cheese',
        'tomato',
        'bacon'
      ])
      // this.eat(hamburger)  
    }

  server:
  mrSlow: